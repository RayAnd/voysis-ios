language: swift
osx_image: xcode9
before_install:
- brew install carthage
script:
  carthage update
script:
  xcodebuild -scheme VoysisTests -project Voysis.xcodeproj -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone X,OS=11.0' build test
after_success:
  - bash <(curl -s https://codecov.io/bash)
